{
  "name": "edm-scr-prescreen",
  "version": "1.0.0",
  "private": true,
  "description": "EveryDayMeds SCR Pre-Screen Chrome extension",
  "scripts": {
    "dev": "web-ext run --target chromium --source-dir . --keep-profile-changes"
  },
  "devDependencies": {
    "web-ext": "^8.2.0"
  }
}
